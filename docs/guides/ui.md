# 界面说明（逐屏 UI 走查）

> **适用版本** `dev@036cf68`（2026-09-15） · **schema** v28 · **最后校对** 2026-09-15
> 版本会前进：`dev@036cf68` 只是本目录最后一次校对的基线；当前适用版本以
> [docs/tasks/README.md](../tasks/README.md) 的最新 FOUNDATION 记录为准。

本文逐个标签页说明 Web UI 上**实际渲染**的东西：每个标签页能看到什么、每个按钮与输入做什么、
哪些数字是**只读投影**、哪些操作会**真的改 Runtime 状态**。

> 本文与 `apps/ui/src/**` 的组件与文案逐个对照写成（核对方法见
> [docs/tasks/README.md](../tasks/README.md) 的 FOUNDATION-078）。**没有凭印象的条目**。
> 界面观感（配色、间距、窄屏排布）不在本文范围，见 [acceptance-checklist.md](./acceptance-checklist.md)。

---

## 0. 一句话前提

Web UI 是**本地 Runtime 的便利前端**，不是另一个产品：

- 界面向 `POST /api/command` 发送的请求体，与 CLI 通过 Unix socket 发送的是**同一个 Zod schema**；
- 事件走 `GET /api/events` 的 SSE 流，与 `events tail` 是同一个订阅；
- 界面**不新增业务语义、不绕过门禁、不直接访问 SQLite**。

启动方式与地址见 [manual.md](./manual.md) §2.4 或 [getting-started.md](./getting-started.md) §5。

### 打开与令牌

- 地址形如 `http://127.0.0.1:<port>/#token=<32字节hex>`。token 放在 **fragment** 里——fragment 不发服务器，
  所以它不会进入任何服务端日志。
- 浏览器把它存进 **sessionStorage**（key 与本次 Runtime 启动绑定）。没有 token 时显示一个只要求粘贴令牌的
  连接表单：「请再次运行 `codeestra ui` 并打开所显示的地址，或在此粘贴 `#token=…` 片段中的令牌。」
- **Runtime 重启会换内存 token**：旧的带 token 链接会立刻失效。
- 关闭页面**不会**停止 Runtime 或任何 Task。

> 图：`00-token-form.png` — 无令牌时的连接表单：标题 Codeestra、说明文字、令牌输入框与「连接」按钮，
> 右上角有「外观」下拉框（此时只是即时预览，不写入任何地方）。

---

## 1. 常驻外壳（每个标签页都一样）

这一层不属于任何标签页，但它决定了你在哪一屏。**标题栏、左侧导航与底部停靠条不随主内容滚动**
（FOUNDATION-072）——只有中间的工作区滚动。

### 1.1 跳转链接与 dev 横幅

| 元素 | 文案 | 说明 |
|---|---|---|
| skip-link | `跳转到工作区` | 键盘用户的第一个焦点；跳到 `<main id="workspace">` |
| dev 横幅 | `开发版 DEV` / `非稳定代码：这是 dev clone 的运行结果，不要当作稳定版；改动请与稳定界面自行对照。` | **只有 dev 构建**（`VITE_CODEESTRA_CHANNEL=dev`）才渲染；`role="status"`，纯提示、**没有任何控件** |

### 1.2 顶部标题栏

| 元素 | 文案 | 行为 |
|---|---|---|
| 品牌 | `C` 图标 + `Codeestra`（dev 构建为 `Codeestra DEV`）+ 副标题 `让意图成为成果` | 无交互 |
| 项目选择器 | 下拉框，`aria-label="当前项目"`，空项显示 `未选择项目`，其余是已接入项目名 | **改状态**：切换项目会清空当前选中的任务、任务列表、待处理列表与所有横幅 |
| 刷新 | 按钮 `刷新` | 重新加载项目列表、权限模式、任务列表、待处理列表，并重读当前选中任务的详情。不重启 Runtime |

### 1.3 左侧导航（`aria-label="主导航"`，上方有 `工作空间` 小标题）

七个标签页，**顺序与文案固定**：

| # | 标签 | 备注 |
|---|---|---|
| 1 | `任务工作台` | 默认标签 |
| 2 | `待处理` | 有待回答请求时显示数字角标（只数 `OPEN` 的） |
| 3 | `调度` | |
| 4 | `项目` | |
| 5 | `Agent 设置` | |
| 6 | `运行事件` | |
| 7 | `设置` | |

窄屏（≤850px）下这一栏变成横向可滚动的一条，`工作空间` 小标题被隐藏。

### 1.4 侧栏底部（三个只读指示）

| 元素 | 文案 | 说明 |
|---|---|---|
| 外观 | 标签 `外观` + 下拉框 `aria-label="界面主题"` | **改状态**：写入 Runtime 的 `theme` 设置（与「设置」标签页的同一项是同一个值） |
| 权限模式 | `FULL · 全权限，零确认` 或 `STRICT · 严格模式` | **只读**：显示当前模式，**界面不提供切换**（切换用 CLI `permission set`） |
| 事件流 | `● 事件流 · 实时` / `正在重连（第 N 次）` / `连接中` / `已停止` | **只读**：断线自动重连，用上次的**排他游标**续订，所以不会重复也不会漏 |

> 图：`13-narrow-layout.png` — 窄屏（≤850px）下的外壳：标题栏保持不动、导航变成横向一条、
> 侧栏底部的三个指示折行显示。

### 1.5 页面标题区

- 左侧：小字（eyebrow）显示**当前项目名**，没选项目时显示 `开始使用`；右侧大字（`h1`）是当前视图名：
  - 任务工作台：没选任务时是 `任务列表`，选了任务时是 `任务详情`；
  - 其余标签页就是标签名本身。
- 右侧灰字：正常情况下是 `本地运行 · 关闭页面不影响任务`；有操作在进行时变成 `{正在做的事}…`。
  **它是一句状态提示，不是一个进度条**——界面不预估百分比。

### 1.6 横幅

- 出错时在最上方出现**错误横幅**（`role="alert"`）+ `关闭` 按钮。错误文案统一形如
  `{操作名}：{CODE}: {message}`，`CODE` 是 Runtime 的稳定错误码。
- 提示类信息（例如「已写入终端输入」）出现**notice 横幅**（`role="status"`）+ `关闭` 按钮。

### 1.7 底部页脚

固定一行：`关闭页面不会停止任务 · 暂停与终止请在任务详情操作 · 合入 dev 不等于发布到 main。`
若选了任务，句尾追加 ` 当前选择：任务 #<编号>。`

> 这一行是刻意的：它把三个最容易误判的事实放在每一屏都能看到的地方。

### 1.8 新建任务停靠条（选了项目才出现）

底部常驻，`aria-label="新建任务"`。两种形态：

**收起时**（一行）：

| 元素 | 文案 | 行为 |
|---|---|---|
| 输入框 | `aria-label="新任务内容"`，占位 `描述一项具体的改动，回车即创建草稿` | 回车或点按钮即创建 |
| 创建按钮 | `＋ 创建草稿`（创建中显示 `正在创建…`） | **改状态**：发 `task.create`，项目为当前选中的项目 |
| 展开 | `展开 ⌃` | 切到详细形态并自动聚焦文本域 |

**展开时**：

| 元素 | 文案 | 行为 |
|---|---|---|
| 标题 | `新建任务 · 详细设定` | |
| 创建按钮 | `＋ 创建草稿` | 同上 |
| 收起 | `收起 ⌄` | 切回收起形态 |
| 规格文本域 | 占位 `描述要完成的改动：目标、范围与验收方式` | 6 行；`⌘/Ctrl + Enter` 创建，`Esc` 收起 |
| 提示行 | `⌘/Ctrl + Enter 创建 · Esc 收起。草稿不会自动启动 Agent，仍需先提交为就绪。` | 只读 |
| 约束 | 图例 `约束`；空时提示 `还没有约束。每一项都是 Agent 必须遵守的具体限制，会作为规格的一部分保存。` | 每行一个输入框 `aria-label="约束 N"`（占位 `例如：不要改动 apps/ui 之外的文件`）+ `删除` 按钮；底部 `＋ 添加约束` |
| 任务类型 | 下拉框 `任务类型`，选项 `DEVELOPMENT · 开发任务`（可选）与 `SELF · 自演进（未实现）`（**禁用**） | 旁边的说明写着 `SELF 尚未实现：Runtime 没有隔离的 self worktree，也没有 Candidate/Stable 隔离；命令行 task create --kind SELF 同样会被拒绝。` |

**创建成功后**：界面**自动切到任务工作台**并选中这条新草稿，同时清空搜索与筛选——这样「刚建的任务」
不会被筛选条件藏起来。

> 从任何标签页都能创建草稿：停靠条在七个标签页里都在。

---

## 2. 任务工作台（`任务工作台`）

任务相关的一切都在这里。它是**列表 + 详情**两个视图，同一时刻只显示一个。

### 2.1 没选项目时

一张空状态卡片：`＋` / `从一个项目开始` / `添加本地 Git 仓库，然后描述你想完成的改动。` /
按钮 `前往添加项目`（切到「项目」标签页）。

### 2.2 任务列表视图

> 图：`03-task-workbench.png` — 任务列表：顶部四个计数卡、搜索与筛选行、任务行（编号 + 规格摘要 +
> 元信息 + 状态徽标 + 提示文字 + 「查看详情 →」）。

**（a）项目任务概况（`aria-label="项目任务概况与快捷筛选（不含归档）"`）**

四个按钮，每个显示一个计数：`全部任务` / `执行中` / `需要你处理` / `成果已提交`。
点任意一个会：设置对应的筛选、**清空搜索框**、**取消勾选「含归档」**。

- `需要你处理` 的定义是：该任务有 `OPEN` 的请求，**或**它的状态是 `WAITING_FOR_USER` /
  `RECOVERY_REQUIRED` / `FAILED`。

**（b）任务列表卡片**

| 元素 | 文案 | 行为 |
|---|---|---|
| 标题 | `任务 <可见数> / <总数>` | 只读 |
| 同步指示 | `● 状态随事件更新`（事件流实时）或 `● 实时连接不可用 · 当前为最近记录，请刷新` | 只读。第二句是**如实**的：连接不可用时界面不会假装还活着 |
| 搜索框 | `aria-label="搜索任务"`，占位 `搜索任务内容或 #编号` | 按规格正文与 `#编号` 过滤；`#3` 这样的写法会被规范成 `3` |
| 状态筛选 | `aria-label="按任务状态筛选"`：`全部状态` / `需要你处理` / 出现过的每个状态 | 只影响显示 |
| 排序 | `aria-label="任务排序"`：`默认顺序` / `最近更新优先` / `优先级从高到低` | 只影响显示 |
| 含归档 | 复选框 `含归档` | 勾选后把归档任务也列出来（**不是**删除，只是默认隐藏） |
| 重置 | `重置`（只在搜索/筛选/排序/归档有任意一项非默认时出现） | 恢复四项默认 |

**任务行**（每行是一个按钮，点开进入详情）：

- 头部：`#<编号>`（等宽字体）、`已归档`（如适用）、`N 项待处理`（该任务有 N 条 `OPEN` 请求时）
- 规格摘要（最多两行）
- 元信息：`规格 r<revision 号>`、`优先级 <n>`（提示写着「数值越大优先级越高；只影响后续调度，不抢占」）、
  `N 条约束`（有约束时）、更新时间（`title` 里带完整更新与创建时间）
- 状态徽标（见下表）+ 一句提示文字
- `查看详情 →`

**状态徽标文案与提示**（源码核对）：

| 状态 | 徽标 | 提示 |
|---|---|---|
| `DRAFT` | 草稿 | 尚未提交 · 提交后进入调度 |
| `READY` | 就绪 / 待调度 | 等待调度或手动启动 · **不代表 Agent 已运行** |
| `RUNNING` | 执行中 | 查看会话、实时步骤或待提交成果 |
| `WAITING_FOR_USER` | 等你处理 | 查看问题或权限请求，回答后继续 |
| `BLOCKED` | 等待依赖 | 上游成果尚未满足依赖条件 |
| `PAUSING` | 正在暂停 | 等待协作停止与静止确认 |
| `PAUSED` | 已暂停 | 现场已保留 · 可继续执行 |
| `CANCELLING` | 正在终止 | 等待协作停止 · 尚未确认结束 |
| `CANCELLED` | 已终止 | 记录保留 · 不会自动重开 |
| `EXECUTED` | 成果已提交 | 查看任务验证与 dev 集成 · **尚非发布** |
| `SUCCEEDED` | 已合入 dev | 已完成任务集成 · **不等于 main 已发布** |
| `FAILED` | 执行失败 | 查看失败原因与执行记录 |
| `RECOVERY_REQUIRED` | 需要恢复 | 状态需人工核对 · **不自动重试** |

徽标的动效（`执行中` / `正在暂停` / `正在终止` 的状态点）描述的是**Runtime 记录的任务状态**，
`title` 里明确写着「不代表进程心跳或完成比例」。

**（c）空列表时**

`从一个任务开始`（完全没有任务，提示去底部创建草稿）或 `没有匹配的任务`（有任务但被筛掉，
带一个 `重置筛选` 按钮）。

### 2.3 任务详情视图

> 图：`04-task-detail.png` — 任务详情：「返回任务列表」、`任务 #N` 标题与状态徽标、
> `下一步` 提示行、任务操作按钮组、规格正文与约束列表。

**（a）标题与规格**

| 元素 | 内容 |
|---|---|
| 返回 | 按钮 `← 返回任务列表`（键盘导航后焦点会自动回到这里） |
| 标题 | `任务 #<编号>` + 状态徽标 + 视情况出现的 `会话已退出 · 等待提交成果` / `已归档` |
| 小字 | `规格 r<revision 号> · 状态版本 v<version>` |
| 规格正文 | 等宽块，就是当前 revision 的 specification |
| 约束 | 约束列表（没有约束时不显示） |

**（b）「下一步」提示行**

一行灰字，按当前状态给出**下一步该做什么**。原文（源码核对）：

| 条件 | 文案 |
|---|---|
| 已归档 | 任务已归档，记录与现场保留；可在「更多操作」中取消归档。 |
| 有待处理请求 | 有待处理请求，请在下方回答。其他任务不受影响。 |
| `DRAFT` | 提交后进入自动调度，满足依赖、冲突与容量条件才会启动。创建草稿不会自动运行。 |
| `READY` | 任务等待调度；可手动尝试启动，Runtime 会核对依赖、冲突与容量，不保证立即运行。 |
| `BLOCKED` | 正在等待上游依赖满足。展开下方任务依赖，查看尚未满足的条件。 |
| 有进行中集成 | 集成尚未完成，请查看下方独立集成验证与合入记录。 |
| 验证进行中 | 任务验证进行中。下方显示实际步骤，可请求取消；完成前不能合入 dev。 |
| 可捕获成果 | Agent 会话已退出。若有代码变更，可提交成果，然后独立验证。 |
| `EXECUTED`、无集成 | 成果已提交。先在固定 commit 上运行任务验证；验证通过后才能合入 dev。 |
| `EXECUTED`、验证通过 | 验证已通过，可以合入 dev。合入会产生独立集成验证，并只在通过后移动 dev 引用。 |
| 已集成 | 已合入 dev。dev → main 的稳定提升是另一条流程，不在这一步内。 |
| `SUCCEEDED` | 成果已合入 dev；这不等于已提升到稳定的 main。 |
| `RUNNING` | 查看下方执行过程；可暂停（保留现场、稍后继续）或终止。 |
| `PAUSED` | 任务已暂停，provider 进程已确认退出；「继续」会在同一工作树新建一次执行并复用该会话。 |
| `CANCELLED` | 任务已终止，不会自动重开；需要重做请新建任务。 |
| `RECOVERY_REQUIRED` | 执行状态需要人工检查，请展开执行与验证记录查看原因；不会自动重试。 |
| `FAILED` | 本次执行失败，请查看执行记录中的错误原因。 |

**（c）任务操作（`aria-label="任务操作"`）——每个按钮做什么**

按钮**按当前状态出现或隐藏**，所以同一个任务在不同时刻看到的按钮数量不同。

| 按钮 | 何时出现 | 发的命令 | 说明（`title`） |
|---|---|---|---|
| `提交为就绪` | `DRAFT` | `task.submit` | 进入调度 |
| `Agent` 下拉框 | `READY` 或 `PAUSED` | — | 选项目前只有 `pi`；**选它决定后续 `task.run` / `task.resume` 用哪个 adapter** |
| `启动 Agent` | `READY` | `task.run` | 走与自动调度同一道门禁 |
| `暂停` | `RUNNING` / `WAITING_FOR_USER` | `task.pause` | 协作停止 provider 进程并确认静止；保留工作树与会话，可用「继续」在同一工作树恢复 |
| `继续` | `PAUSED` | `task.resume` | 在同一工作树新建一次执行，并复用已暂停会话的 provider conversation |
| `提交成果`（FULL）| 可捕获时，见下 | `task.result.capture` | 提交工作树中的成果，Runtime 会再次核对静止证据与差异 |
| `准备成果提交`（STRICT）| 可捕获时 | `task.result.prepare` | 之后出现「成果提交授权」区块 |
| `验证任务` / `重新验证` / `验证进行中…` | `EXECUTED` | `task.verify`（`background: true`） | 在固定 commit 上独立运行验证；后台运行，可在此查看进度并取消 |
| `合入 dev` | 见下 | `task.integrate` | 把成果 commit 合入 dev：先在独立工作树里合并，再跑独立集成验证，通过后才移动 dev 引用 |
| `更多操作` → `终止` | 多数状态 | `task.cancel` | 终止是**终态**；协作停止 Agent，保留工作树与全部记录 |
| `更多操作` → `归档` / `取消归档` | 见下 | `task.archive` / `task.unarchive` | 归档**只隐藏任务**，不删除记录或回收工作树；可随时取消归档 |
| `更多操作` → `重试（task retry）` | **总是可见**（不做本地状态判断） | `task.retry` | 重新入队这个任务并请调度门禁尝试启动一次；被拒绝时显示 Runtime 返回的稳定码 |

**（c2）`更多操作` 里的重试块（`task retry`）**

`更多操作` 展开后，除 `终止` / `归档` 两个按钮外，还有一个重试块（源码位置：`apps/ui/src/task-retry.tsx`）。
它的要点是**界面不重做命令面的状态判断**：按钮**只受「是否正在发请求」控制**，不会因为任务不是 `FAILED` 而隐藏或禁用；
被拒绝时界面显示 Runtime 实际返回的稳定码。

| 元素 | 内容（固定文案） |
|---|---|
| 标题 | `重试失败任务`（小字 `task retry · 会真的改状态`） |
| 说明 | 只对 `FAILED` 生效；其它状态返回 `TASK_NOT_FAILED` / `TASK_CANCELLED` / `TASK_STILL_RUNNING` / `TASK_PAUSED` / `RECONCILE_REQUIRED` / `TASK_ARCHIVED`；按钮不做本地状态判断；重试会重新入队并发起「一次」启动请求，在同一条调度门禁后面排队 |
| `当前版本` | `v<version>`——它会被原样作为 `expected-version`（CAS）发送；界面显示过期时返回 `CONCURRENT_MODIFICATION` |
| `上一次运行的 Adapter` | 取该任务**最新一次执行记录**的 `adapterId`；这就是不带 `--adapter` 时命令面复用的那一个（`adapterSource: RECORDED`） |
| `这次使用的 Adapter` | 下拉框：默认项 `沿用该任务上一次运行的 Adapter（<id>）`（无记录时为 `（无记录：回退到默认 Adapter）`），其余选项来自 `runtime.ping` 的 `adapters`；读不到时如实显示 `（读取失败：<错误码>）` |
| 按钮 | `重试（task retry）`；发送 `task.retry`（选定 adapter 时才带 `adapterId`） |

发完请求后出现的三种结果**分开呈现**（只有第一种是启动）：

| 结果 | 界面 | 含义 |
|---|---|---|
| `STARTED` | 绿色 `已启动` + 「新执行已启动（命令面退出码 0）」 | 真的新建了 Execution；卡片另列 execution / session / 工作树路径 |
| `WAIT` | 警示色 `已入队 · 在等待（退出码 3）` | 重试已记录、任务已重新入队，但**这次没有启动任何执行**；卡片列出等待理由（容量/冲突）与占着位置的 Task |
| `REFUSED` | 红色 `已入队 · 启动被拒绝` | 重试已记录，但这次启动被拒绝（如 `DEPENDENCIES_UNMET`）；卡片列出稳定码与未满足依赖 |

三种结果都额外显示 `已重新入队：state <READY | BLOCKED> · 版本 v<n> · 重试记录 <id>`、Adapter 及来源
（`你这次指定` / `沿用该任务上一次运行的 Adapter` / `回退到默认 Adapter`）、工作树模式与启动答案的 `code`/`detail`。
**没有启动时卡片不显示 execution / session 行**。而**命令本身**被拒绝（例如任务不是 `FAILED`）时，错误横幅显示形如
`重试被拒绝：TASK_NOT_FAILED: <Runtime 的说明>（<本地词汇表的解释>）`——稳定码始终原样可见。

三个「什么时候出现」的判据（源码核对）：

- **可捕获成果**：任务 `RUNNING`、最近一次 Execution 仍在 `RUNNING`、它仍持有资源、
  **且它的 Session 已经是 `EXITED`**。此时标题旁还会出现 `会话已退出 · 等待提交成果`。
- **可以合入 dev**：任务 `EXECUTED`、存在一次 `PASSED` 的任务验证且它的 revision 与 result commit
  都**对得上当前事实**、且**没有**已完成的集成批次、也**没有**进行中的集成。
- **可以归档**：非归档状态，且状态不是 `RUNNING` / `WAITING_FOR_USER` / `PAUSING` / `PAUSED` /
  `CANCELLING` / `RECOVERY_REQUIRED`。「更多操作」底部的说明原文：
  `终止后不能重开；归档只隐藏任务，不删除记录或回收工作树。`

**（d）待处理请求（就地嵌入）**

若该任务有 `OPEN` 请求，任务详情里会**直接嵌入**「需要你的回答」卡片（与「待处理」标签页是同一个组件，
只是不带「任务 #n」跳转按钮）。回答完不需要离开这一屏。

**（e）长命令进度**（有 Operation 时才出现）

标题 `长命令进度` + 小字 `Runtime 记录的事实步骤`。折叠块 `进度与取消说明` 的原文：
`只显示实际步骤，不预估百分比。事件流中断时每 5 秒刷新进度。取消是协作停止，确认静止后才结束；无法确认则保留占用并需要人工处理。`

表格列：`类型` / `状态` / `最新步骤` / `更新时间` / `操作`。

- `类型` 显示 `Agent 运行`（`RUN_TASK`）、`任务验证`（`RUN_TASK_VERIFICATION`）或原始 kind，下面一行是
  Operation ID 前 8 位。
- `状态` 是 Operation 的状态；若结果里记了 `cancelled: true`，则显示 `已取消（用户）`——**用户的取消不报成「失败」**。
- `最新步骤` 显示最新一步的 `step` 与状态；若收到过输出进度，另起一行显示
  `命令 … · stdout N B / stderr N B · 已运行 Ns · 仍在输出`（**只有大小与时长，没有输出内容**）。
  有结果时还有一个 `结果详情` 折叠块显示原始 JSON。
- `操作` 对进行中的 Operation 给出 `取消` 按钮（`task.operation.cancel`，`title` 写明
  「只在该进程组确认静止后才记录终态；运行类会协作暂停任务」）；终态显示 `—`。
- 折叠块 `全部步骤` 逐条列出每一步：`step` + 状态 + 时间 + 详情 JSON。

**（f）成果提交授权（只有 STRICT 且已 prepare、且版本没变时出现）**

标题 `成果提交授权`，说明：`请在确认差异符合预期后再继续。HEAD 或变更集发生任何变化都会使此授权失效。`
键值表：`预期 HEAD` / `变更指纹` / `已静止`（是/否）/ `工作区`。按钮 `确认成果提交`
（`task.result.commit`，`confirm: true`），**只有在 `已静止` 为「是」时才可点**。
FULL 下这个区块**不出现**。

**（g）原始响应与最近一次验证**

- `最近一次任务运行 · 原始响应`：折叠块，显示 `task run` / `task resume` 的原始 JSON。
- `最近一次验证：<状态>（<结果码>）`：折叠块，显示验证证据 JSON。

**（h）执行失败提示与验证摘要**

- 最近一次执行有错误时出现红字：`执行失败：<code> <message>`。
- 有验证记录时出现一行摘要：`最近验证` + 状态徽标 + 被验证 commit 前 10 位 +
  `<结果码 或 "等待结果"> · 不代表集成或发布`。

**（i）会话结束注记**（有注记时才出现）

小字 `FOUNDATION-056`。它解释**一次本来无法解释的 `SUCCESS`**：Agent 结束轮次的形态被 Runtime 标注了。

每条显示：第几次执行、记录结果（成功/失败）、失败码（若有）、注记码（例如
`PROSE_QUESTION_NO_TOOL_USE`）、一句人读摘要，以及事实行
`工具调用 N · 最后文本截断 是/否 · 停止原因 <…>`，再加一个折叠块显示 Runtime 记录的原始注记。

> **边界**：这一块**不声称** Agent 在等你回答。它说的是「这次结束的形态需要注记」。

**（j）执行、验证与集成记录（可折叠）**

折叠标题带计数：`执行、验证与集成记录 · N 次执行 / N 次验证 / N 次合入`。展开后是三张表 + 一段说明：

| 表 | 列 |
|---|---|
| 执行记录 | `#`（第几次）/ 状态 / 适配器 / 模型·思考 / 会话 / 占用资源 / 基线（前 10 位）/ 失败原因 |
| 验证记录 | 状态 / 结果 / 提交（前 10 位）/ 策略 digest（前 10 位）/ 结束时间 |
| 集成批次 · dev | 批次（前 10 位 + `N 个成员` + `devRef`）/ 状态 / dev 基线（前 10 位）/ 合入后 dev（未改动时显示 `未改动`）/ 合并（`fast-forward` 或 `merge commit` + 合并提交前 10 位）/ 集成验证（前 10 位）/ 结果码（有 `detail` 时在下面一行）/ 时间 |

空表各有自己的文案：`暂无执行记录。` / `暂无验证记录。` /
`本任务还没有集成批次；成果不会自动进入 dev。`（「项目」标签页的同一张表用的是 `这个项目还没有集成批次；成果不会自动进入 dev。`——空列表在两个位置的含义不同）。

集成批次表的每一行下面**紧跟一张成员表**（`成员任务` / `revision` / `结果提交` / 成员状态 /
`合入其中的 commit` / `说明`），**按 `task_id` 排序**——这是 ADR-0053 的成员顺序，所以一个跨任务的批次
不会只显示第一个成员。状态文案由 `apps/ui/src/integration-batches.tsx` 给出：`PREPARING` 是「正在合并成员」
（批次的 `PREPARING` 是合并，与 Execution 的 `PREPARING`（在启动进程）不是一回事）、`INTEGRATED` 是
「已合入 dev」、`STALE`/`CANCELLED` 写明「未合并、dev 未动」/「未合入」，`RECOVERY_REQUIRED` 写明
「未收口（成员仍被占用）」。表下方一行小字提醒「批级 INTEGRATED 只说明已合入 dev，不等于已进 main」。

**只读**：这张表与它的成员表里没有任何按钮或输入框（组批/集成/取消在「项目」标签页，见 §5.3）。

第三张表的小标题是 `集成批次 · dev`，带小字 `只读 · 一个批次可以跨多个任务（ADR-0053）`；
表后一行小字：`成员按 task_id 顺序列出；批级 INTEGRATED 只说明已合入 dev，不等于已进 main。`

> 本节的表格由任务详情与「项目」标签页的 `集成批次` 面板共用同一个组件，所以两处的列与文案一致。

**（k）Agent 会话与执行过程**

标题 `Agent 会话与执行过程` + 小字 `只读过程 + 原生终端（同一 CLI 命令面）`。

- 没启动过 Agent 会话时，明确写着：`这个任务还没有启动过 Agent 会话，因此没有执行过程可显示。`
- 否则先有一个 `执行` 下拉框（列出**有 Session 的**每一次执行：`第 N 次 · 状态 · 会话状态`），
  然后是两个面板：

**「原生终端与会话交接」面板**（`ADR-0023 / ADR-0026 · 同一 CLI 命令面`）

键值表：`会话`（会话 id 前 8 位 · 会话状态 · 执行状态 · 权限模式）、`Provider 会话`（provider session id ·
会话文件名）、`当前 incarnation`、`写入租约`、`side channel`。
若存在 STRICT 权限请求，会多一行提示，原文写明：`请在「待处理」页面回答；终端输入不能批准它。`

`安全点与 fence` 小节：要么 `已到安全点`（fence 已被 provider 确认、无活动工具、fence 后有 settled 事实、
无未决 Attention），要么 `未到安全点` 并列出**仍缺什么**；下面四条事实：
`fence 已确认` / `活动工具` / `fence 后出现 settled 事实` / `未决 Attention`。
**安全点只由结构化事实判定，不从终端屏幕文本推断。**

按钮（都会**真的改状态**）：

| 按钮 | 命令 | `title` |
|---|---|---|
| `请求接管` | `session.handoff.request`（kind `TAKEOVER`） | 持久化接管意图并装上 fence；不会中止正在运行的工具 |
| `接管（启动原生终端）` | `session.handoff.admit` | 由 Runtime 判定：安全点、predecessor 归属、Execution 仍为 RUNNING 全部通过才会启动原生终端 |
| `取消接管请求` | `session.handoff.cancel` | 放弃接管请求并释放 fence，Agent 可以继续使用工具 |
| `刷新状态` | `session.handoff.status` | 只读 |

接管结果以一个横幅显示（成功为 notice / 失败为 error），带 `code`、`detail`、predecessor 核验、
传输、successor 是否启动。

`Incarnation 历史` 小节：小字写明「同一 conversation 的进程代号，**不是同一个进程**」。表格列
`#` / 模式 / 状态 / provider pid / 记录的后代 / 前身 / 开始 / 结束。

`原生终端` 小节：

- 没有终端时：`这个会话还没有原生终端。接管（上一步）成功启动 provider 后，这里会出现终端的 PTY、游标与附加状态。`
- 有终端时显示键值表：`终端`（id · 状态 · 本 Runtime 是否持有）、`进程`（helper pid · provider pid · pty slave）、
  `窗口`（`已应用`/`未应用` 的**初始**尺寸应用事实，另起一行显示
  `ptyResize: <取值> · <一句解释>`）、`投影游标`（cursor · 保留字节 · 已产生字节 ·
  是否有界缓冲丢弃过）、`当前写入者`。
  - `窗口` 行的 `ptyResize` 取值**直接来自** `session.handoff.status` 的能力矩阵，**不硬编码、不改写**：
    `IMPLEMENTED`/`SUPPORTED` → `窗口大小可以改变`；`UNSUPPORTED` → `窗口大小不能改变`；
    `PARTIAL` → `只在部分平台上可改变`（具体平台范围以命令面与 ADR 为准）；`UNVERIFIED` → `尚未验证`；
    词汇表以外的取值只原样列出并明确写「本界面没有这个取值的词汇表，不作解释」。
    命令面没有报告该项时显示「命令面没有报告 `ptyResize` 这一项」——**不会假装可用或不可用**。
    因此这一行与下方 `能力矩阵` 表里的同一个值总是自洽的。
- 折叠块 `N 条附加记录`：每条显示身份、holder、状态、附加/分离时间与原因。
- 附加控制：`身份` 下拉框（`写入者（每个终端至多一个）` / `观察者（可多个，只读）`）+ holder 文本 +
  `附加` 或 `分离（当前：…）`。`title` 会说明为什么不给附加。
  界面明写：第二个写入者会被 Runtime **稳定拒绝为 `ATTACHMENT_BUSY`** 并报出当前 holder，
  **这里不会排队，也不会伪装成功**。
- 终端投影区：一个等宽只读文本块（`aria-label="终端投影（不可信内容，按文本显示）"`），
  加 `清空显示` 按钮；说明写着「控制字符按可见字符显示（ESC → ␛），不解释 ANSI 序列；内容按不可信文本渲染」。
  若投影被标记为 `truncated`，界面**明说**「中间可能有缺失」。
- 终端输入：输入框（`aria-label="终端输入"`，占位 `终端输入（写入不是审批通道）`，非 writer 时禁用）、
  复选框 `末尾附加回车（CR，等同按 Enter）`、按钮 `写入`。
  **输入框只在以「写入者」身份附加时可用。**
- 交还：复选框 `交还后继续自动化（启动 RPC successor）` + 按钮 `交还自动化（release）`
  （`title` 写明「写入终端自己的 release 字节（Ctrl+D），等待 provider 退出并核验归属与会话文件；
  **不杀 provider，不中止工具**」）。release 结果横幅里写明「退出码只是审计数据，不参与判定
  （Ctrl+D 与 SIGTERM 都可能是 0）」。
- 折叠块 `能力矩阵 · Runtime 自己报告的实现程度`：表格显示 Runtime 报来的每个能力键与值。
  说明：「不支持的必须显式反馈，不静默降级」。

> 图：`07-terminal.png` — 「原生终端与会话交接」面板：会话/incarnation/写入租约/side channel 键值表、
> 「安全点与 fence」四条事实、四个按钮、原生终端投影区与写入行。

**执行过程（transcript）子面板**

它就在同一个 `Agent 会话与执行过程` 区块里、终端面板下方，**没有自己的标题**。

- 工具行：`N 条记录 · <会话状态> · 自动刷新中/已结束`、`排列` 下拉框
  （`正序（最早在前）` / `倒序（最新在前）`）、`刷新` 按钮，以及一句只读说明
  `内容来自 Provider 自己的会话文件，只读展示；不会写入数据库，也不改动任务状态。`
- 每种条目左侧有一个类型标签：`任务输入` / `Agent` / `工具返回` / `切换模型` / `思考深度` / `其他记录`。
  用户自己输入的那条默认展开，其余默认折叠为单行摘要。
- 展开后显示：角色、provider/模型、`stop=…`、`tool=…`、用量（`入 N · 出 N · 推理 N · 合计 N · 成本 N`）、
  条目 id，以及逐段正文。分段类型有 `文本` / `思考` / `工具调用` / `图片` / `其他`；
  被截断的部分显示 `N 字符` 与 `展开全文` / `收起`，并注明「已截断，仅显示前 N 字符」。
- 工具返回条目会标 `工具报错` 或 `工具成功`。
- 还有更多记录时出现 `加载后续记录` 或 `加载更新的记录`（倒序时）。
- 会话文件被替换导致游标失效时，界面会明确提示 `会话文件游标已失效，已从头重新读取。`

> 图：`06-transcript.png` — 「Agent 执行过程」面板：排列下拉框、单行时间线条目、
> 展开后的分段正文与「展开全文」按钮。

**（l）依赖与稳定提升投影**

同一屏里还有两个面板：

- **`依赖与 BLOCKED 原因`**（小字 `只读投影 · 不写任务状态`）：显示 `dev ref` 与 commit、
  该任务的状态、`刷新` 按钮；若被阻塞则有一个红色区域列出每条未满足原因（原因码 + 前置任务 + revision），
  外加一张表（`前置任务` / `已合入 commit` / `判定` / `原因` / `集成批次`）与一行上下游闭包。
  说明原文：`一条边只有在钉住的上游 revision 有真正到达 INTEGRATED 的合入、且该 commit 仍可从当前 dev
  到达时才算满足。BLOCKED 只表示依赖未满足；冲突等待、容量等待与 revision 等待不会被算成 BLOCKED。`
- **`稳定提升记录 · dev → main`**（小字 `只读 · ADR-0022 / ADR-0052`）：只列出**包含本任务**的提升记录；
  表格列 `状态` / `候选 commit` / `dev 基线` / `main 结果` / `模式` / `重启` / `结果` / `时间` / `详情`。
  `状态` 单元格里有**两个分别的标记**：`state`（Runtime 的状态机）和**派生的 `phase`**——
  `尚未推送` / `已推送、等待拉取` / `待重启核对` / `重启已记录、远端 main 未发布` / `已完成` / `记录已失效或未推进`。
  只有 `已完成`（`phase: COMPLETE`）用成功色，其余阶段用等待/失败色调。另外两列各多一行**读回值**：
  `origin/dev <SHA>`（push 后 `git ls-remote` 读回来的）与 `origin/main <SHA>`（推回并读回后才非空）。
  说明原文：
  `提升是另一条流程：合入 dev 不等于提升到 main，「已推送远端 dev」也不等于已提升——推送之后还需要你在 main 检出执行 git fetch origin + git merge --ff-only origin/dev，再次运行 promotion promote 才会核对、记录并执行重启，最后推回远端 main。main 已移动也不等于 Runtime 已完成重启。这里只显示记录里的事实。`
  `phase` 为 `已推送、等待拉取` 时：`main 结果` 列显示 `尚未拉到 main 检出`（不是「未改动」），`重启` 列显示 `此阶段不记录`。
  `详情` 打开一个只读详情卡：`阶段（派生）`、`远端读回`（两个 SHA + 推送/推回时间 + dev clone 路径，
  并注明它们是读回来的观察值而不是输入）、`Runtime 重启` 的逐步表，以及一个 **`下一步`** 块。
  `下一步` 按 `phase` 给出下一件真事：`AWAITING_PULL` 时标题为
  `已推送 ≠ 已提升：候选已在远端 dev，main 检出还没有拉取`，下面是一个命令块
  （`cd <检出 main 的那个 clone>` / `git fetch origin` / `git merge --ff-only origin/dev`），
  并写明「本界面不执行 Git、不拉取、也不重启；这两条命令只能在 main 检出里由你执行」，
  且「命令面在这一阶段退出码 3——那是等待，不是失败，也没有任何重启记账」；
  `RESTART_PENDING` 时列出接下来会跑的四步（`bun install --frozen-lockfile` / `bun run build:ui` /
  `bun run codeestra stop` / `bun run codeestra status`）；`MAIN_PUSH_PENDING` 时说明重跑只重试推回。
  这个面板**不发任何命令**：它只读 `promotion.list` / `promotion.get`。

**（m）调度判定与影响投影**

- **`调度判定`**（小字 `task schedule explain · 只读，不启动任何东西`）：显示判定（决策）、任务状态、
  是否候选、adapter、`刷新`；等待块（类型/原因码/已等待多久/谁占着/命中路径）；
  未满足依赖列表；评估块；以及一个 **UNKNOWN 的显式单次放行**区域。
  - **唯一会写状态的东西**是这个区域里的红色按钮 `记录单次放行（task schedule clear-unknown）`
    （`title`：`记录一次绑定上述 revision 与评估版本的单次放行；它不启动任务`）。
    它只在判定确实是 `UNKNOWN` 且当前没有有效放行时可点。
  - 界面明写：**风险由放行方承担**；放行**不改变判定记录**（该次 assessment 仍是 `UNKNOWN`，不是 `SAFE`）；
    放行后失效条件包括任务修订、基线变化、映射/分析器/策略版本变化、实际 diff 超出预测。
- **`影响与冲突判定`**（小字 `project impact show / explain · 只读`）：
  - 按钮 `刷新快照`（`project.impact.show`）与 `解释判定（explain）`（`project.impact.explain`）——**都只读**。
  - `候选快照`：任务状态、处置（disposition）、revision、工作区基线与项目 dev 基线是否一致、
    路径大小写实测，然后是快照详情：完整性（`完整（可参与 SAFE）` / `不完整（complete=false，不可能是 SAFE）`）、
    变更指纹、changed 路径（可展开）、命中重要目录/模块/全局资源、映射未分类的路径、证据行。
  - 无法派生快照时界面**明说**：`无法派生快照：… ——没有快照就意味着无法排除与它的重叠，相关判定只能是 UNKNOWN。`
  - `判定`：verdict 徽标、对比了多少个活跃/预留任务、逐活跃任务表（任务/状态/执行/快照/不完整原因/记录）、
    已落库的逐配对判定（append-only）、判定解释与判定证据（可折叠）。

---

## 3. 待处理（`待处理`）

> 图：`05-attention.png` — 「待处理」标签页：标题 `需要你的回答` 与数量角标、`刷新` 按钮，
> 一张问卷卡片（题号、题干、单选/多选选项、自定义回答输入框），底部「发送 N 个回答」与「拒绝回答」。

没选项目时显示 `请选择一个项目。`。否则是一张卡片：

| 元素 | 文案 | 行为 |
|---|---|---|
| 标题 | `需要你的回答` + 数字角标（`OPEN` 的数量） | 只读 |
| 刷新 | `刷新` | 重新读 `attention.list` |
| 说明 | `只暂停对应任务。回答提交后由 Runtime 投递给 Agent，不需要离开工作台。` | 只读 |

每条 `OPEN` 请求渲染成一张卡（整体是一个 `fieldset`，`aria-label="回答 Agent 请求"`）：

| 元素 | 说明 |
|---|---|
| 头行 | 按钮 `任务 #<编号>`（或 `查看关联任务`）——**会跳回任务工作台并选中该任务**；`kind` 徽标（`问题` / `权限请求` / `恢复`）；responseType 文字（`确认` / `文本`）；创建时间 |
| 问卷（`QUESTION` + 问卷形态） | 逐题显示 `N. [题头] 题干`（多选时追加 `（可多选）`），每个选项是一个单选框/复选框，标签为 `N. 选项名 — 选项说明`；每题下面还有 `第 N 题：自定义回答` 输入框（占位 `或用自己的话回答（会覆盖上面的选择）`）。**同一题上「选项」与「自定义文本」互斥**，所以发出去的东西不会含糊 |
| 问卷底部按钮 | 主按钮 `发送 N 个回答`——**一个问题都没答时不可点**，文案变成 `请至少回答一个问题`（未答的问题会作为「未回答」而不是「拒绝」上报）；危险按钮 `拒绝回答` |
| 非问卷请求 | 先显示请求的原始 JSON（等宽块） |
| `responseType = CONFIRM` | 两个按钮：`允许` / `拒绝` |
| 其他（`VALUE`） | 输入框 `回答内容`（占位 `输入回答`）+ 按钮 `发送` |
| 非问卷请求底部 | 按钮 `拒绝回答此请求` |
| 请求处理中 | 灰字 `正在发送回答…`，整个 fieldset 被禁用 |

列表下方有一个折叠块 `N 个已回答或已关闭`，逐条列出 `类型 · 状态 · 时间`。

**只读与可写的分界**：这一屏上除了 `刷新`、`任务 #n` 跳转、以及上面的回答控件以外**没有别的动作**；
回答控件发的都是 `attention.answer`，**只有它们会真的改状态**。

---

## 4. 调度（`调度`）

> 图：`08-schedule.png` — 「调度」标签页三张卡片：调度引擎（含活跃集合与候选顺序）、容量与槽位预留、
> 影响映射 · impact.json。

没选项目时显示 `请先选择一个项目。`。否则是**三张卡片**，全部是**只读投影 + 少量显式写操作**。

### 4.1 调度引擎

小字：`只读投影 + 一次显式 tick · ADR-0030/FOUNDATION-055`。说明原文：
`Runtime 自己会按事件与周期 tick 驱动调度。这里显示它的候选顺序与判定；「触发一次调度」只是请求一次与自动 tick
完全相同的循环，不改变判定规则。冲突等待与容量等待都不是 BLOCKED；BLOCKED 只表示依赖未满足。`

| 按钮 | 命令 | 是否改状态 |
|---|---|---|
| `刷新状态` | `task.schedule.status` | 只读 |
| `查看计划（dry run）` | `task.schedule.plan` | **只读**：不预留、不启动、不改变任何状态；界面会出现一个 notice 横幅明确写着「这是 dry run（task schedule plan）／它不预留、不启动、不改变任何状态；显示的是『若现在跑会怎样』。」 |
| `回到 status 视角` | — | 只切显示 |
| `触发一次调度（task schedule run）` | `task.schedule.run` | **改状态**：这可能真的启动任务 |

表格：`adapter` / `调度循环`（`Runtime 内的 tick 循环正在运行` 或 `没有常驻 tick 循环（事件与请求仍会触发）`）/
`draining` / `最近一次 tick`（触发者 + 完成时间）/ `容量`（一行摘要）。

**`活跃集合`**（小字：`占用槽位或正在运行，不会因心跳过期或 UI 关闭而释放`）
表格列：`任务` / `任务状态` / `执行状态` / `adapter` / `预留` / `已运行`。空时 `没有活跃任务。`

**`候选顺序`**（小字：`优先级降序，其次创建时间；提优先级不抢占`）
上方一行汇总：`N 个现在会启动 · N 个等待（冲突 N / 容量 N）· N 个依赖未满足`，
加一个复选框 `只看会启动/等待/阻塞的候选`（只影响显示）。

每个候选是一张卡：

- 头行：`#<编号>`、`优先级 N`、`<状态> · 任务版本 vN`、处置徽标
  （`已启动` / `会启动` / `等待` / `阻塞` / `失败`），若被放行则多一个 `由单次放行允许`。
- 一行 `revision … · adapter … · 创建于 …`。
- 有等待时显示等待块：等待类型徽标（冲突等待 / 容量等待）、原因码、`已等待 N`、原因说明、详情、
  `占用槽位` 或 `占用冲突范围`（列出对方任务）、原因码列表，以及**命中清单**：
  `${reason} ${中文说明} · ${类别} · 对方 #n · 关系 X` + 命中路径（含截断提示）/ 重要目录 / 模块 / 全局资源 + detail。
- 依赖未满足时有一个 `依赖未满足（BLOCKED 的唯一含义）` 小节，逐条列原因码、前置任务、revision。
- 有评估时显示评估块：verdict 徽标、`分析器 <ver> · 映射 <ver>`、被评估的 revision、基线 commit、
  候选快照（完整/不完整 + 不完整原因，并注明「complete=false，因此不可能是 SAFE」）、
  对比过的任务、活跃集合、原因码、解释行。
- 已启动时补一行执行/会话/预留/基线 id。

**`实际影响超出预测`**（小字：`scheduler.md §4：记录增长并请求安全暂停`）
列出每条增长：新增/移除路径数、与几个活跃任务冲突、reason、是否已请求暂停，以及详情。
空时 `没有记录到增长。`

**`最近一次显式 tick`**（点了 `触发一次调度` 之后才出现）
显示 tick id、触发者、起止时间、是否与已在运行的 tick 合并、期间是否 draining，
以及**属于当前项目**的候选处置列表。

### 4.2 容量与槽位预留

小字：`scheduler capacity / reservations · ADR-0032`。说明原文：
`两个上限同时生效：项目全局与每 adapter。容量等待不是 BLOCKED；降低上限不会释放已持有的槽位。
释放必须给出原因，且不会因心跳过期或客户端消失自动发生。`

**容量表**：范围 / 上限 / 来源 / 已用 / 可用 / `现在获取会得到`（`可以获取` 或一条容量等待原因）。
adapter 没有覆写时来源列下注 `跟随全局上限`。表下一行给出配置版本、最后修改时间与修改者；
若「已用 > 上限」会追加一句 `已用大于上限：降低上限不会释放已持有的槽位，事实如实显示`。
Runtime 正在排水时，表上方出现一个红框：`Runtime 正在排水：拒绝新的预留` + 原因。

**`当前占用者`**：每个占用者一行（任务、adapter、自何时、预留 id）。

**`显式设置上限`**：说明里列出非法值的稳定码
（`CAPACITY_LIMIT_INVALID` / `CAPACITY_LIMIT_OUT_OF_RANGE` / `UNKNOWN_ADAPTER`），并写明
`不会被静默夹取；上限范围 1–16`。

| 控件 | 命令 | 说明 |
|---|---|---|
| `范围` 下拉框 | — | `项目全局` 或某个 `adapter <id>` |
| 并发上限输入框（`aria-label="并发上限"`） | — | 输入被**原样**传给 Runtime；界面不替你把笔误夹成「看起来合理」的数字 |
| `设置上限（scheduler capacity set）` | `scheduler.capacity.set` | **改状态** |
| `清除该 adapter 覆写（capacity clear）` | `scheduler.capacity.clear` | **改状态**；`title`：`只有显式设置过的 adapter 覆写才能清除`；范围是「项目全局」时不可点 |

**`槽位预留`**：`刷新`（只读）、复选框 `包含已释放`、
`执行 reconcile（scheduler reservations reconcile）`（**会写审计记录**）。

界面还明写两段容易误解的事实：

- `reconcile 只读真实进程表：已死 → 释放；仍存活 → 保持占用；无法核验 → RECOVERY_REQUIRED。它不发信号、不杀进程、不删资源、不声称静止。`
- 关于槽位移交：`Task 启动后，槽位由预留移交给该 Execution（预留记 EXPLICIT 释放，理由写明
  "execution … now holds the Task resource"），占用靠 Execution 的 resource_held 继续计入容量。
  因此"没有活跃预留而任务在跑"是正常事实，不是预留丢失：打开"包含已释放"可以看到这次移交。`

reconcile 结果卡：`reconcile 观测` + boot id；表格列 `预留` / `任务` / `观测` / `判定` / `状态` / `说明`；
下方一行 `本次没有向任何进程发信号；记录的进程：…`。

预留表列：`任务`（含任务版本与 revision）/ `状态`（含预留版本）/ `adapter` / `版本` / `工作区` /
`评估基线` / `预留在`（含已持有时间）/ `持有者证据`（boot / pid / startToken / actor）/
`释放`（时间、类型、观测、原因）/ `详情` 按钮。

`详情` 打开一个只读卡：`预留 <id> · 审计历史`，显示状态、依赖指纹、影响快照 id，然后逐条列出历史事件
（序号、kind、时间、actor、详情、可展开的「证据」JSON）。**唯一会写状态的东西**是这个卡里的：
输入框 `释放原因`（占位 `释放原因（必填，写入审计）`）+ 危险按钮 `释放预留（需原因）`
（`title`：`显式释放；可证明仍存活的持有者会被拒绝（SLOT_HOLDER_STILL_RUNNING）`，原因为空时不可点）。
`预留状态` 已经是 `已释放` 时，这一组控件不出现。

### 4.3 影响映射 · impact.json

小字：`project impact validate · ADR-0031`。说明原文：
`映射只从项目 main 引用读取，Task 分支上的同名文件不参与判定。映射缺失、无效或未确认时，每一个判定都会是 UNKNOWN（不是「无冲突」）。`

`重新校验` 按钮（`project.impact.validate`，**只读**）+ 仓库根路径。

结果区：校验码徽标 + 中文说明、`main <ref> @ <commit>` / 分析器版本 / `已信任为 <name>`（或「此仓库尚未被信任」）、
映射摘要（映射状态、确认状态、是否生效、main 引用、策略与内容摘要、声明数量），
无效时多一块错误码与错误信息，最后是警告列表与项目 id。

> **注意**：这个面板在**「调度」标签页**里，不在「项目」标签页里。

---

## 5. 项目（`项目`）

### 5.1 添加本地项目

标题 `添加本地项目`，说明：`输入 Git 仓库的绝对路径，检查仓库与验证策略后添加。不会修改仓库文件。`

| 元素 | 文案 | 行为 |
|---|---|---|
| 主路径输入 | `aria-label="Git 仓库绝对路径"`，占位 `/仓库/路径` | 改动它会清空下面已检查出的结果（以及信任被拒绝的提示） |
| dev clone 路径输入 | `aria-label="dev clone 绝对路径"`，标签 `dev clone 路径（必填）`，占位 `/另一个检出 dev 的 clone` | **必填**（CLI 的 `--dev-repo`，字段 `project.trust` 的 `devRepoPath`）；改动它同样清空已检查出的结果 |
| 检查项目 | `检查项目` | **只读**：并发发 `project.inspect`（带上填好的 `devRepoPath`）与 `project.verificationPolicy` |

dev clone 路径输入下面固定一行小字（原文）：`与 CLI 的 --dev-repo 同一个字段（project.trust 的 devRepoPath）：稳定提升会用这个 clone 把固定候选推到远端 dev。值原样放进请求；能不能用由 Runtime 核验 —— 拒绝时这里显示它返回的 DEV_REPO_* 稳定码与解释。`；该输入为空时（含只有空格）额外出现一行提示：`还没有填 dev clone 路径：project.trust 需要它，所以下面的「添加项目 / 信任项目」按钮不会启用（不会假装成功）。`（带 `data-project-trust="dev-repo-missing"`）。

检查结果分两段：

- **仓库身份**键值表：`仓库根目录` / `main 引用` / `对象格式` / `HEAD`，加两行开发基线相关的新行：
  - `dev 基线`：`refs/heads/dev <commit 前 12 位>`（不存在时写 `DEV_REF_MISSING` 会被拒）。
  - `dev clone（这次会记录）`：键值表里的一项，内容**全部来自 `project.inspect` 返回的核验结果**，界面不自己判断路径：路径、徽标 `已核验（这个 clone 可以作为该项目的 dev clone）` 或 `未通过核验 · <稳定码>`、Runtime 的 `detail`、稳定码对应的本地解释，以及核验读到的事实（dev 分支 / HEAD 的 ref / dev ref commit / HEAD / 工作树干净 / origin 与主检出相同 / origin）。`clean` 与 `originMatchesProject` 为 `null` 时写 `未核验`（“没核实到”，不是“否”）。
  - 未给 dev clone 路径且项目也没记录过时，这一行写 `未指定：检查时没有给 dev clone 路径，也没有已记录的路径。` 与 `信任需要它（CLI：project trust … --dev-repo <path>）。`。
- **验证策略**：`ABSENT` 时显示
  `main 引用上没有策略。您仍可信任此项目，但在 .codeestra/policies/verification.json 存在之前，验证会被拒绝。`；
  存在时是一张表 `ID` / `命令` / `工作目录` / `超时`（秒），下面是 `main <sha12> · 摘要 <sha12>`。

两段都拿到之后才出现第三个区块：

| 权限模式 | 标题 | 说明 | 确认 | 按钮 |
|---|---|---|---|---|
| `FULL` | `添加此项目` | `全权限模式已默认开启：Agent、未知工具、验证命令和 Git 钩子均以当前用户权限运行，不再请求确认。` | 无 | `添加项目` |
| `STRICT` | `信任此项目` | `严格模式下，信任后 Agent、验证命令和 Git 钩子可使用您的用户权限运行，但提交和未知工具仍受门禁。` | 输入框 `aria-label="输入 TRUST 以确认信任"`，占位 `输入 TRUST 以确认`；不输入 `TRUST` 按钮不可点 | `信任项目` |

按钮上方固定一行小字：`这个按钮会发出 project.trust：把你审阅的身份（含上面的 dev clone 核验结果）、验证策略 digest 与 dev clone 路径一起提交。dev clone 路径为空时按钮不可点 —— 信任需要它，界面不假装成功；路径能不能用由 Runtime 核验。`

**按钮的可用条件只有两条**（都能在界面上看到原因）：dev clone 路径非空，且在 `STRICT` 下已输入 `TRUST`。
按钮发的是 `project.trust`，并**把刚刚看到的身份（含 dev clone 核验结果与 dev 基线）与策略 digest 一起提交**（防漂移：这期间它们变了会被拒为 `REPOSITORY_CHANGED` / `VERIFICATION_POLICY_CHANGED`）。若被拒绝，按钮下方出现红字 `信任被拒绝：<稳定码>: <Runtime 说明>（<本地词汇表一句解释>）`——**稳定码逐字保留**，词汇表覆盖 `DEV_REPO_*` 家族（含 `DEV_REPO_REQUIRED`）与信任面本身的拒绝码。成功后清空确认输入并重新加载项目列表。

> 图：`02-project-trust.png` — 「项目」标签页的「添加本地项目」区块：路径输入与「检查项目」、
> 仓库身份表、验证策略命令表、以及底部的添加/信任区块。

### 5.2 项目级依赖与稳定提升记录

选了项目之后，第二张卡片里有三个面板（不绑定某个任务）：

- `依赖与 BLOCKED 原因`：项目级的依赖图，**按依赖任务分组**，表格列
  `依赖任务` / `前置任务` / `已合入 commit` / `判定` / `原因` / `集成批次`。
- `集成批次 · dev`：这个项目所有批次的**项目级视图**（含跨任务的多成员批次），见 §5.3。
- `稳定提升记录 · dev → main`：这个项目的提升记录（最多 20 条，超出时界面会写明
  `最多显示最近 20 条，可能还有更早的记录。`），列与状态/阶段标记、读回值与 `详情` 卡（含 `下一步`）
  都与任务详情里的一致；同样是**只读**投影，不发任何命令。

### 5.3 项目级集成批次（`集成批次 · dev`）——本页唯一能改集成状态的地方

标题 `集成批次 · dev`，小字 `同一 CLI 命令面 · ADR-0018 / ADR-0053`。这个面板把已经存在的批次命令面
（`task integration list|create|integrate|cancel`）投影进界面；它**不新增业务语义**，也不绕过任何门禁。

**（a）只读部分**

说明原文（两段）：

- `这是项目级的批次记录：一个批次可以有一个或多个成员任务，按 task_id 顺序合并，然后跑一次覆盖整批的独立集成验证，只有 PASSED 才推进 dev。`（其中「一次」是 `<strong>`），紧接着是固定提醒句 `批级 INTEGRATED 只说明这批成员的成果已合入 dev，且 dev ref 已被 CAS 推进。它不等于「已进 main」：稳定提升是另一条流程，需要在 main 检出里拉取并重启稳定 Runtime（见下方「稳定提升记录 · dev → main」）。合入 dev ≠ 已发布。`
- 两条「命令面没有的事实」说明：批级没有「进度百分比 / 剩余时间」这类字段（状态就是唯一的进度事实：`CREATED` → `PREPARING` → `VERIFYING` → `INTEGRATING_DEV` → 终态）；成员行只记录批次组成的时间，不记录成员自己的开始时间，所以界面**不显示**「成员耗时」。

然后是 `刷新批次记录`（标注 `只读`；读 `task.integration.list`）与批次表：列与文案与任务详情里的
`集成批次 · dev` 完全相同（见 §2.3（j）），每个批次行下面挂着按 `task_id` 排序的成员表。

**（b）`组批（task integration create）`区块**

小字 `会写一条批次记录 · 不碰 Git`。说明写清了：create 把每个成员当前的 revision、成果 commit 与整批的
`dev` 基线固定下来，不合并、不验证、`dev` 不动；成员按 `task_id` 排序；契约上限
`32` 个成员（显示为事实，界面**不**用它当门禁）；同一个任务写两次或一个成员都不给会被 Runtime 拒为
`INVALID_REQUEST`。

接着是一段小字（在下拉框之前、按钮之前）：

- `每个成员行上显示的 v<version> 就是这次要发送的 expected-version（CAS）；它过期时 Runtime 会拒绝为 CONCURRENT_MODIFICATION，请刷新后重试。下拉框列出项目里每一个任务，不按状态过滤，也不隐藏按钮：能不能当成员由 Runtime 判断。`

成员选择是一个可重复的 `成员 N 的任务` 下拉框（列出**当前项目里每一个任务**，
`#编号 · 状态 · v版本 · 规格前 60 字`——**不按状态过滤**），每行右侧写清这次会发送的
`expected-version v<n>`（CAS），行尾是 `移除`；列表下面是 `＋ 增加一个成员`。

然后是 `<pre>` 请求预览，即这一份请求的字段（按对象顺序逐行：`command` / `commandId` /
`projectId` / `members[i].taskId` / `members[i].expectedVersion`；`commandId` 显示为 `（点击时新生成）`，
因为真值每次点击才生成）。再下面是按钮 `组批（task integration create）`。

结果卡分两种：`批次已组成（未碰 Git）` 与 `返回既有批次（created: false）`（命令重放），卡里列出
`batchId`、固定的 `devRef`/`devCommit` 与成员表；被拒绝时是红字 `组批被拒绝：<码>: <Runtime 说明>（<本地词汇表一句>）`。

**（c）`批次的集成与取消`区块（唯一会真的改状态的地方）**

每个批次一张卡：批次 id 前 10 位、状态徽标、成员 id 列表、一行「按记录里这条批次的状态（…）」的说明，
然后是两个控件。**按钮不按本地允许清单隐藏或禁用**：批次处于任何状态都渲染两个按钮，只有「有一个请求在飞」
时禁用；能不能集成/取消由 Runtime 判断，被拒绝时逐字显示它返回的稳定码。

| 控件 | 按钮上方的原文（每个按钮正上方都有一句） |
|---|---|
| `集成（task integration integrate）` | `这个按钮会做（task integration integrate）：按 task_id 顺序合并全部成员，然后对最终提交跑一次覆盖整批的独立集成验证，只有 PASSED 才用 CAS 把 dev 从记录里的基线推进到合并提交。失败、冲突、成员证据移动或 dev 基线移动都不会推进 dev；已经是终态的批次不会重跑，只返回既有记录。` |
| `取消（task integration cancel）` | `这个按钮会做（task integration cancel）：尝试结束这个批次。只有在记录能证明它还没有任何副作用（仍是 CREATED，且没有 worktree、没有合并、没有验证）时才会真的变成 CANCELLED；否则命令面把它改为 RECOVERY_REQUIRED / RECONCILE_REQUIRED（退出码 3）——没有被取消、成员继续被占用，需要人工按记录处理。所以取消不保证成功，也从来不会推进 dev。FULL 与 STRICT 下都是零确认。` |

取消控件还有一个可选的 `取消原因` 输入框（`可选，1–1000 字符；留空则不发送 reason`）。

结果卡有三种，且带 `data-integration-verdict` 属性：

| 情况 | 卡片 | `data-integration-verdict` |
|---|---|---|
| integrate 真的推进了 `dev` | `已合入 dev`（绿）+ 退出码 0 + 合并提交/dev 推进到/整批验证状态 + `不等于「已进 main」` | `INTEGRATED` |
| integrate 是命令重放 | `此前已合入（本次未重跑）` | `ALREADY_INTEGRATED` |
| integrate 得到 `STALE`/`CANCELLED`/`FAILED`/`CONFLICTED` | `未合入`（非绿）+ 退出码 1 + 结果码 + `dev 未推进（integratedCommit 仍为 null）` | `NOT_INTEGRATED` |
| integrate 得到 `RECOVERY_REQUIRED` | `未收口 · 需要人工对账` + 退出码 3 | `NEEDS_RECONCILIATION` |
| cancel 成功 | `已取消` + 退出码 0 | `CANCELLED` |
| cancel 未能证明无副作用 | `未被取消 · 需要人工对账` + `没有被取消（命令面退出码 3）` | `RECOVERY_REQUIRED` |
| cancel 遇到已终态批次 | `批次已是终态` + 幂等 + 退出码 1 | `ALREADY_TERMINAL` |
| 任一命令被拒绝 | 红字 `<组批/集成/取消>被拒绝：<码>: <Runtime 说明>（<本地词汇表一句>）` | `REFUSED` |

integrate 的结果卡还会列出 `dev 基线` / `合并提交` / `合入后 dev` / `整批的集成验证`（`verificationId`
与状态，并注明「任务验证 ≠ 集成验证」）/ `工作树` / `结果`，加上本次返回的验证命令表（`argv`/退出码/耗时/输出字节）
与验证树摘要；重放既有记录时没有命令结果，界面如实写 `这次调用没有返回验证命令的结果（重放既有记录时不重跑命令）。`。

面板末尾一行小字：不提供「删除批次」或「重试合并」；失败/失效的批次保留现场、按当前事实重新组批。
不做 `promotion` 的任何动作（不推送、不拉取、不重启）。

> 这个面板**不**调用 `task integration get`：`list` 返回的记录带每个成员的全部字段
> （含 `taskVersion`/`devCommit`/`createdAt`/`completedAt`），`get` 的 `members` 没有这几列；
> 一次读取就够，且同样的记录任务详情里也在用。

---

## 6. Agent 设置（`Agent 设置`）

这一页是 Agent 配置与插件选择的**唯一**界面入口。说明原文：
`adapter、模型、思考深度与插件选择都在这里配置，走的是与 CLI 同一条命令面。修改只对新建 Session 生效，
不重启 Runtime、不需要确认；每次执行都会把当时生效的插件列表（路径 + 类别 + 来源层）写进 Execution 记录。`

若已勾选第三方 extension，页面顶部会出现一段 ⚠ 说明，原文：
`⚠ 已选择第三方 extension。Pi 的 fail-closed 审批门禁靠 --no-extensions 保证「唯一被加载的 extension 是
Codeestra 自己的」，用户显式加载的第三方 extension 可能影响或绕过该审批。本轮不新增审批层、也不做运行期拦截，
这一事实会被记录到 Execution。`

| 控件 | 选项 | 说明 |
|---|---|---|
| `Adapter` | `pi` / `codex` / `claude` | 切换会重新读取配置与插件候选 |
| `作用域` | `全局默认` / `项目覆盖` | 没选项目时**不可点**，并显示 `未选择项目：只能编辑全局默认。` |

若该 adapter 的能力投影报告 `pluginSelection: UNSUPPORTED`，页面会明说
`该 adapter 本轮不支持插件开关，因此不显示候选列表，也不会把选择写进配置。`（Codex 与 Claude 当前就是这种情况。）

**`当前生效值（<adapter>）`** 表：`字段` / `值` / `来源`。字段有 `Provider`、`模型`、`思考深度`，
以及四个插件类别（`扩展（extensions）` / `技能（skills）` / `提示词模板（prompt templates）` / `主题（themes）`）。
来源的显示值是 `环境变量` / `项目覆盖` / `全局默认` / `适配器默认`；值是「适配器默认」或
「未选择（不加载）」时会直说。

> **边界**：这一页**不自己算优先级**——`effective` 与 `sources` 都由 Runtime 返回，所以界面不可能与
> 任务实际会跑的东西不一致。

若本进程的环境变量正在覆盖这些字段，会多一段说明列出具体是哪些字段与值，并写明
`环境变量优先级最高（环境变量 → 项目覆盖 → 全局默认 → 适配器默认），但只属于本次 Runtime 进程，
改它要重启 Runtime；这里保存的值会被它盖住。`

**`插件候选（只读检测）`**：说明写明来源目录、是否可读、以及
`检测只读 provider 自己的用户配置目录，不扫描仓库内的 .pi/、.claude/、.codex/，也不写任何配置。`
按四个类别分组，每项一个复选框 + 名称 + `路径 · 来源 X · provider 已启用/未启用`；
不可选的项会追加 `· 不可启用：<原因>`（原因是 Runtime 的稳定码，未知码原样显示，不猜含义）。

**`编辑并保存（<作用域>）`**：

| 控件 | 说明 |
|---|---|
| `Provider` 输入框 | 占位 `留空 = 适配器默认` |
| `模型` 输入框 | 占位 `留空 = 适配器默认` |
| `思考深度` 下拉框 | `适配器默认` / `off` / `minimal` / `low` / `medium` / `high` / `xhigh` / `max` |
| 勾选说明 | `已勾选 N 个插件路径。保存会以这组勾选整体替换该作用域的选择（不是与低优先级作用域合并）；取消全部勾选等于「不加载任何插件」。路径不可核验时 Runtime 会拒绝保存并给出稳定错误码，此处不会替你静默丢弃。` |
| `保存` | `agent.config.set`——**改状态** |
| `清除选择` | `agent.config.set` + `pluginSelection: null`——**改状态** |
| `清除该范围的模型配置` | `agent.config.clear`——**改状态** |

> 图：`10-agent-settings.png` — 「Agent 设置」标签页：adapter 与作用域、当前生效值表（字段/值/来源）、
> 插件候选勾选列表、编辑并保存区与三个按钮。

---

## 7. 运行事件（`运行事件`）

> 图：`09-events.png` — 「运行事件」标签页：状态指示与游标、跟随/清空按钮、以及带
> sequence / eventType / aggregateType / payload 的事件帧列表。

标题 `Runtime 事件流 · 全部项目`。

| 元素 | 文案 | 行为 |
|---|---|---|
| 状态 | `实时`（绿）或其他连接状态 | 只读 |
| 游标 | `游标 <n>` 或 `游标 —` | 只读 |
| 跟随 | `停止跟随` / `继续跟随` | 只切本地订阅开关，不改 Runtime 状态 |
| 清空 | `清空` | **只清空本地已显示的帧**，不动事件日志 |

说明原文：`帧来自 CLI 使用的同一订阅。游标采用排他语义，因此使用此处显示的游标重连时，
既不会重复也不会遗漏事件。`

每个事件帧显示：`sequence`（等宽灰）、`eventType`（等宽）、`aggregateType`（灰）、
payload 的 JSON（等宽、单行截断）。调度类事件额外显示一句人读摘要，并注明
`（原始 payload 即为左侧 JSON，未被改写）`。

> **边界**：这一屏**不写任何业务状态**。清空与停止跟随都只影响这个浏览器标签页。

---

## 8. 设置（`设置`）

> 图：`11-settings.png` — 「设置」标签页的「界面效果」：五个键各一行（中文名 + 键名 + 说明 +
> 下拉框 + 当前/默认/是否显式设置 + 恢复默认），以及每行的等价 CLI 命令。

标题 `界面效果`，右侧按钮 `重新读取`（读取中显示 `正在读取…`）。

说明原文：`设置存放在 Runtime（CODEESTRA_HOME）里，不是浏览器本地存储：换浏览器、清理浏览器存储或重启
Runtime 后依然生效，命令行（codeestra settings ui …）读写的是同一份值。`

成功后显示：

- 设置文件的**真实路径**（等宽）与一句 `appliesTo` 说明。
- 五个键各一行：

| 列 | 内容 |
|---|---|
| 名称 | 中文名（`主题` / `密度` / `字号` / `动效` / `时间显示`）+ 等宽的键名 |
| 说明 | 一句该键的作用（例如主题：「跟随系统会实时跟随操作系统的浅色/深色外观。」） |
| 下拉框 | `aria-label="<中文名>（<键名>）"`，选项来自 **Runtime 报来的可取值**（界面不自己声明取值集合） |
| 状态字 | `当前 <值> · 默认 <值> · 已显式设置` 或 `未显式设置（使用默认值）` |
| `恢复默认` | **改状态**：`settings.ui.reset` + 该键；未显式设置时不可点 |
| 等价命令 | 每行下方显示 `codeestra settings ui set <key> <value>`——界面不新增 CLI 没有的能力 |

底部还有 `全部恢复默认`（`settings.ui.reset` 不带 key，**改状态**；没有任何显式设置时不可点），
说明原文：`这只会删掉 Runtime 里记录的显式选择，不删除文件本身，也不改变任何任务状态。`

最后一行提示：`外观选择仍可从侧栏的「外观」下拉框修改，两处写的是同一个设置。`

读写失败时显示错误横幅 `读取或写入 Runtime 设置失败：<CODE>: <message>`。

---

## 9. 汇总：只读投影 vs 真的改状态

这是本文最重要的一张表。**看起来像操作面的只读投影**最容易让人误判。

### 9.1 纯只读（点多少下都不会改 Runtime 状态）

| 位置 | 元素 |
|---|---|
| 任务工作台 | 搜索 / 状态筛选 / 排序 / 含归档 / 重置 / 任务行 / 返回 / 各折叠块 / 执行·验证·集成记录 / 会话结束注记 |
| 任务工作台 | `集成批次 · dev` 表与它下面的成员表（没有按钮/输入框） |
| 任务工作台 | `依赖与 BLOCKED 原因`（整块）、`稳定提升记录 · dev → main`（整块，含 `详情`、`phase` 标记、`origin/dev`/`origin/main` 读回值与 `下一步` 块） |
| 任务工作台 | `更多操作` 里重试块的**只读部分**：`当前版本` / `上一次运行的 Adapter` / `这次使用的 Adapter` 下拉框（只改本地选择，不发请求）/ 结果卡；`runtime.ping` 的 adapter 列表读取 |
| 任务工作台 | `影响与冲突判定` 的 `刷新快照` 与 `解释判定（explain）` |
| 任务工作台 | 原生终端面板的 `刷新状态`；`Agent 执行过程` 的 `刷新` / `排列` / 展开与加载更多 |
| 待处理 | `刷新`、`任务 #n` 跳转、已回答/已关闭折叠块 |
| 调度 | 调度引擎的 `刷新状态`、`查看计划（dry run）`、`回到 status 视角`；候选顺序的显示开关 |
| 调度 | 容量表的全部数字、`当前占用者`、`包含已释放` 开关、`刷新`；预留表的 `详情` |
| 调度 | `影响映射 · impact.json` 的 `重新校验` |
| 项目 | `检查项目`；两段检查结果 |
| 项目 | `集成批次 · dev` 的只读部分：`刷新批次记录`、批次表与成员表、每条批次的状态说明、`组批` 区的成员下拉框（只改本地选择，不发请求）、请求预览、结果卡 |
| Agent 设置 | `当前生效值` 表、`插件候选` 列表、adapter 与作用域下拉框 |
| 运行事件 | 全部（含 `停止跟随` / `清空`，它们只影响本标签页的显示） |
| 设置 | `重新读取`、五行的下拉框**当前值回显**（改它才写） |

### 9.2 真的改 Runtime 状态

| 位置 | 元素 | 命令 |
|---|---|---|
| 外壳 | 项目选择器 | 只切界面上下文（**不写 Runtime**） |
| 外壳 | `刷新` | 只重新读取；**不会重启 Runtime**（那要 CLI `stop`） |
| 外壳 | `外观` 下拉框 | `settings.ui.set`（key `theme`） |
| 停靠条 | `＋ 创建草稿` | `task.create` |
| 任务详情 | `提交为就绪` | `task.submit` |
| 任务详情 | `启动 Agent` / `继续` | `task.run` / `task.resume` |
| 任务详情 | `暂停` | `task.pause` |
| 任务详情 | `提交成果` / `准备成果提交` / `确认成果提交` | `task.result.capture` / `task.result.prepare` / `task.result.commit` |
| 任务详情 | `验证任务` / `重新验证` | `task.verify`（background） |
| 任务详情 | `合入 dev` | `task.integrate` |
| 任务详情 | `取消`（长命令行） | `task.operation.cancel` |
| 任务详情 | `终止` / `归档` / `取消归档` | `task.cancel` / `task.archive` / `task.unarchive` |
| 任务详情 | `更多操作` → `重试（task retry）` | `task.retry`（重新入队 + 一次启动请求；事件记录后仍可能只是等待或启动被拒） |
| 任务详情 | 原生终端：`请求接管` / `接管` / `取消接管请求` / `附加` / `分离` / `写入` / `交还自动化` | `session.handoff.request` / `admit` / `cancel` / `attach` / `detach` / `terminal.write` / `release` |
| 任务详情 | 调度判定：`记录单次放行` | `task.schedule.clearUnknown` |
| 待处理 | `发送 N 个回答` / `允许` / `拒绝` / `发送` / `拒绝回答…` | `attention.answer` |
| 调度 | `触发一次调度（task schedule run）` | `task.schedule.run` |
| 调度 | `设置上限` / `清除该 adapter 覆写` | `scheduler.capacity.set` / `clear` |
| 调度 | `执行 reconcile` / `释放预留（需原因）` | `scheduler.reservations.reconcile` / `release` |
| 项目 | `添加项目` / `信任项目` | `project.trust` |
| 项目 | `组批（task integration create）` | `task.integration.create`（只写一条批次记录：不合并、不验证、不碰 Git；同一个任务写两次或没有成员会被拒为 `INVALID_REQUEST`） |
| 项目 | `集成（task integration integrate）` | `task.integration.integrate`（合并全部成员 + 一次整批的独立集成验证；只有 PASSED 才 CAS 推进 `dev`） |
| 项目 | `取消（task integration cancel）` | `task.integration.cancel`（只在记录证明无副作用时真正取消；否则 `RECOVERY_REQUIRED`，退出码 3） |
| Agent 设置 | `保存` / `清除选择` / `清除该范围的模型配置` | `agent.config.set` / `agent.config.set`（清选择）/ `agent.config.clear` |
| 设置 | 五个下拉框（改动时）、`恢复默认`、`全部恢复默认` | `settings.ui.set` / `settings.ui.reset` |

**注意 `刷新` 不是 `重启`**：界面**不提供**停止或重启 Runtime 的控件（那是 CLI `stop` / `status`），
也**不提供**权限模式切换（那是 CLI `permission set`）。

---

## 10. 界面 vs CLI：哪些只在一边

界面是 CLI 的**子集**。按第一原则，「只有 UI 能做」的能力才算缺陷；反过来「只有 CLI 能做」是明确允许的，
因为 CLI 必须完备。以下能力当前**只有 CLI**：

| 能力 | 现状 |
|---|---|
| `attention resolve`（散文提问等待的回应） | 界面尚无该控件；等待会以 `WAITING_FOR_USER` + 会话结束注记显示，退出方式在 CLI |
| `settings prose-question-attention` | 全局开关，界面未提供（「设置」标签页只有界面效果五项） |
| `reclaim plan/apply/records` | 破坏性命令面，界面未提供 |
| `promotion prepare/approve/promote/abandon` | 界面上的稳定提升记录是**只读**的 |
| `task tests record/show/history` | 界面只显示验证结果与策略摘要 |
| `project knowledge validate/list/show/resolve` | **界面完全没有投影**（`apps/ui/src/**` 里没有 knowledge 相关组件） |
| `task revision create/list` 与 `task revision delivery list/get/resolve` | 界面未提供专用视图 |
| `task depends add/remove` | 界面只显示依赖（`task depends list` 的投影），不提供增删 |
| `session handoff writer acquire/release`、`terminal read` | 界面终端面板提供附加/输入/交接的主要动作，完整控制面在 CLI |
| `scheduler reservations acquire/release/prepare-workspace` 的获取与准备 | 界面提供容量设置、预留查看、释放与 reconcile；预留的获取/准备控制面在 CLI |
| `agent plugins select` 的 flag 形式（`--extension` / `--clear`） | 界面提供等价的勾选与「清除选择」 |
| `stop` / `permission set` / `status` | 界面只显示权限模式与事件流状态；停止与切换用 CLI |
| `events list` | 界面只做实时订阅 |

`events.subscribe` 与 `runtime.ui` 这两个命令在 HTTP 上会被明确拒绝（`NOT_AVAILABLE_OVER_HTTP`）：
界面用 `/api/events` 做事件，用启动时的地址做 UI 端点，而不是通过命令面。

---

## 相关阅读

- 从头读到尾的说明书：[manual.md](./manual.md)
- 功能清单：[features.md](./features.md)
- CLI 完整命令参考（含 `/api/command`、`/api/events`）：[cli-reference.md](./cli-reference.md)
- 常见任务的做法：[recipes.md](./recipes.md)
- 人工观感核对清单：[acceptance-checklist.md](./acceptance-checklist.md)
- 插图清单：[images/README.md](./images/README.md)
- UI 相关的 ADR：[0007](../decisions/0007-local-web-ui-entry.md)、
  [0013](../decisions/0013-read-only-agent-transcript-view.md)、
  [0015](../decisions/0015-task-workbench-and-themes.md)、
  [0017](../decisions/0017-new-task-dock.md)、
  [0031](../decisions/0031-impact-snapshot-and-deterministic-conflict-analyzer.md)、
  [0034](../decisions/0034-compact-task-workbench.md)、
  [0045](../decisions/0045-global-ui-settings.md)、
  [0049](../decisions/0049-dev-ui-channel-marker.md)
